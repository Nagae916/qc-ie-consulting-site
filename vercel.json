{
  "ignoreCommand": "git diff --quiet $VERCEL_GIT_COMMIT_REF^ $VERCEL_GIT_COMMIT_REF -- \
  . ':(exclude)README.md' ':(exclude)**/*.md' ':(exclude)**/*.mdx' ':(exclude)**/*.css' \
  ':(exclude).github/**'"
}
