// src/types/global.d.ts

/* eslint-disable unused-imports/no-unused-vars */
/**
 * グローバル Window の型拡張。
 * eslint の「未使用変数」警告対象になるため、このファイルだけ無効化しています。
 */
declare global {
  interface Window {
    twttr?: any;
  }
}

export {};
