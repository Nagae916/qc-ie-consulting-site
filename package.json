{
  "scripts": {
    "dev": "next dev -p 3000",
    "build": "next build",
    "start": "next start -p 3000",
    "lint": "next lint",
    "check:routes": "node ./scripts/check-routes.js",
    "check": "npm run lint && npm run check:routes"
  },
  "devDependencies": {
    "husky": "^9.1.6",
    "lint-staged": "^15.2.9"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx,json,md,mdx}": [
      "npm run lint -- --fix",
      "git add"
    ]
  }
}
